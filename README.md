# mem - A simple, console-based spaced repetition flashcard program

A simple flashcard program that runs in the terminal.  See the manpage for
usage information.  To build, just run `make`.  To install, run `make install`
with sufficient privileges after building.
